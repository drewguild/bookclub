<%= form_with(url: "/sessions/#{session[:user_id]}", method: "delete") do %>
    <% submit_tag("Logout") %>
<% end %>

<h1>Book Nominations</h1>

<ul id="nominated-books">
    <%= render(@books) %>
</ul>

<%= link_to("Nominate a new book", new_nomination_path) %>
